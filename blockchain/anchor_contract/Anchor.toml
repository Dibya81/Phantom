[toolchain]
anchor_version = "0.29.0"
solana_version = "1.18.0"

[features]
seeds = true
skip-lint = false

[programs.devnet]
phantom_anchor = "PhAnToMiDpRoGrAmIdPLACEHOLDER11111111111111"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
