# PhantomID — Unified Ecosystem Dependencies
# Optimized for Render Free Tier (512MB RAM)
# Using the latest FastEmbed (ONNX) which supports Python 3.12+

# ── AI LAYER (Lightweight & High Performance) ──
fastembed>=0.4.0
llama-index-embeddings-fastembed>=0.2.0
llama-index

# ── CORE ──
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-dotenv==1.0.1
pydantic>=2.7.4
jsonschema==4.22.0

# ── AGENT ──
langgraph==0.1.9
langchain-core>=0.2.43
langchain-groq==0.1.5
groq

# ── DATA ──
llama-index-vector-stores-postgres
supabase==2.5.0
psycopg[binary]
sqlalchemy

# ── BLOCKCHAIN ──
anchorpy==0.21.0
solana>=0.36.1,<1.0
solders>=0.21.0,<1.0

# ── INTEGRATION ──
httpx==0.27.0
twilio==9.0.5
google-api-python-client==2.134.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.0

# ── TESTING ──
pytest>=8.2.0
pytest-asyncio>=0.23.6
